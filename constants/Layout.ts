/**
 * Design System — SafePlay Timer Layout & Spacing
 */

const Layout = {
    // ── Spacing Scale ──────────────────────────
    spacing: {
        xs: 4,
        sm: 8,
        md: 12,
        lg: 16,
        xl: 24,
        xxl: 32,
        xxxl: 48,
    },

    // ── Border Radius ──────────────────────────
    radius: {
        sm: 4,
        md: 8,
        lg: 12,
        xl: 16,
        xxl: 20,
        xxxl: 32,
        round: 999,
        full: 9999,
    },

    // ── Screen Padding ─────────────────────────
    screen: {
        paddingHorizontal: 24,
        paddingTop: 16,
    },

    // ── Child Mode Card Sizes ──────────────────
    childCard: {
        minHeight: 140,
        iconSize: 56,
        borderRadius: 20,
    },

    // ── Parent Card Sizes ──────────────────────
    parentCard: {
        minHeight: 80,
        borderRadius: 12,
    },

    // ── PIN Lock ───────────────────────────────
    pin: {
        dotSize: 16,
        dotSpacing: 16,
        keySize: 72,
        keySpacing: 16,
    },

    // ── Header ─────────────────────────────────
    header: {
        height: 56,
    },

    // ── Bottom Tab Bar ─────────────────────────
    tabBar: {
        height: 64,
    },
};

export default Layout;
