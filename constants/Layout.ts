/**
 * Design System — Layout & Spacing Constants
 */

const Layout = {
    // ── Spacing Scale ──────────────────────────
    spacing: {
        xs: 4,
        sm: 8,
        md: 16,
        lg: 24,
        xl: 32,
        xxl: 48,
    },

    // ── Border Radius ──────────────────────────
    radius: {
        sm: 8,
        md: 12,
        lg: 16,
        xl: 24,
        round: 999,
    },

    // ── Screen Padding ─────────────────────────
    screen: {
        paddingHorizontal: 20,
        paddingTop: 16,
    },

    // ── Child Mode Card Sizes ──────────────────
    childCard: {
        width: 280,
        height: 200,
        iconSize: 64,
    },

    // ── Parent Card Sizes ──────────────────────
    parentCard: {
        minHeight: 100,
    },

    // ── PIN Lock ───────────────────────────────
    pin: {
        dotSize: 18,
        dotSpacing: 16,
        keySize: 72,
        keySpacing: 16,
    },
};

export default Layout;
